[project]
name = "sentineleye-schemas"
version = "0.1.0"
description = "Cross-service Pydantic schemas for SentinelEye"
requires-python = ">=3.12"
license = { text = "Apache-2.0" }
dependencies = ["pydantic>=2.9.0"]

[tool.setuptools.packages.find]
where = ["python"]
